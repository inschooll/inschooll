import Login from '@/app/auth/login/page';
import { fireEvent, render, screen} from '@testing-library/react';

describe('Login Page ', () => {
  it('should have form submit when all data in form is valid', () => {
  });
});

